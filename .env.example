# Plantilla de variables de entorno.
# Copia este archivo a ".env" y pon valores reales.
# El archivo .env NO se sube a GitHub (esta en .gitignore).
# Debe existir un .env tanto en tu PC como en el VPS.

POSTGRES_PASSWORD=pon_aqui_una_contraseña_segura
